{
  "name": "chat",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel src/index.html --no-source-maps -p 3000",
    "build": "parcel build src/index.html",
    "start": "parcel build src/index.html && node server"
  },
  "engines": {
    "node": ">=18.0"
  },
  "targets": {
    "default": {
      "engines": {
        "browsers": ">= 30%"
      }
    }
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@parcel/transformer-inline-string": "^2.8.2",
    "handlebars": "^4.7.7",
    "parcel": "^2.8.2"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.2",
    "express": "^4.18.2"
  }
}
